const currencies = ['RUB', 'USD', 'EUR'];
const apiRoot = 'http://localhost:4000';
const defaultCurrency = 'RUB';

const noop = (): void => {
  // do nothing
};

export { currencies, apiRoot, defaultCurrency, noop };
